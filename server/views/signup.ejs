<!DOCTYPE html>
<html>
<head>
    <link type="text/css" rel="stylesheet" href="style.css" />
</head>
<body id="signupBody">

    <div id="navbar">
        <a class="navbarLinks" id="navbarHome" href="/">Kanji Memory Add</a>
        <a class="navbarLinks navbarRightside" href="/signup">sign up</a>
        <a class="navbarLinks navbarRightside" href="/login">login</a>
    </div>

    <form action="/addSignup" method="post">
        <div class="container">
            <h1>Sign Up</h1>
            <hr class="horizontalLine">

            <label>Username</label>
            <input class="wideInput" type="text" placeholder="Username" name="username" required autofocus maxlength="20">

            <label>Email</label>
            <!-- regex, require, special character @ = (?=.*[@]), . = match anything with previous condition checking, min 8 max 20 = {8,20} -->
            <input class="wideInput" type="text" placeholder="Email" name="email" required pattern="((?=.*[@]).{5,30})">

            <label>Password</label>
            <!-- regex, require, one digit = ((?=.*\d), one lowercase = (?=.*[a-z]), one uppercase = (?=.*[A-Z]), . = match anything with previous condition checking, min 8 max 20 = {8,20} -->
            <input class="wideInput" type="password" placeholder="Password" name="password" required pattern="((?=.*\d)(?=.*[a-z])(?=.*[A-Z]).{8,20})">

            <div class="clearfix">
                <button class="wideButton" type="submit">Sign Up</button>
            </div>
        </div>
    </form>


</body>
</html>